<Page
    x:Class="Pesu.Windows.Pages.SummaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light">
    <Grid Padding="48,36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="SUMMARY" Foreground="{StaticResource PesuMutedBrush}" FontWeight="SemiBold" />
            <TextBlock Text="Meeting brief" FontSize="52" FontFamily="Times New Roman" Foreground="{StaticResource PesuInkBrush}" />
        </StackPanel>

        <Border Grid.Row="1" Margin="0,24,0,0" Padding="18" CornerRadius="12" Background="White">
            <TextBlock Text="{Binding SelectedMeeting.Summary}" TextWrapping="Wrap" />
        </Border>

        <StackPanel Grid.Row="2" Margin="0,18,0,0" Spacing="10">
            <TextBlock Text="Decisions" FontSize="18" FontWeight="SemiBold" />
            <ItemsControl ItemsSource="{Binding SelectedMeeting.Decisions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="14" CornerRadius="10" Background="White" Margin="0,0,0,8">
                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Page>
